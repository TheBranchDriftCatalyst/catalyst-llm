apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mac-node

resources:
  - namespace.yaml
  - ollama-external.yaml
  - vllm-external.yaml
  - webui-external.yaml
  - whisper-external.yaml
  - ingressroute.yaml
  # - servicemonitor.yaml  # Uncomment if prometheus-operator is running

labels:
  - pairs:
      app.kubernetes.io/managed-by: mac-node
    includeSelectors: false
