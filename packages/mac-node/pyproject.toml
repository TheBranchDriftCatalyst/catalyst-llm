[project]
name = "mac-node"
version = "0.1.0"
description = "M3 Mac bare-metal LLM inference node services"
requires-python = ">=3.12"

dependencies = [
    "open-webui>=0.8.0",
    "mlx>=0.22.0",
    "mlx-lm>=0.21.0",
]

[tool.setuptools]
py-modules = []

[project.optional-dependencies]
vllm = ["vllm-mlx"]
