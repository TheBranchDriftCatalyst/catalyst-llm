# SillyTavern Deployment - DISABLED
#
# TODO: Reimplement from scratch
#
# Issues encountered:
# - Container tries to write to config.yaml at startup (read-only in image)
# - Various mount strategies failed (ConfigMap, emptyDir+initContainer, subPath)
# - App starts but exits with code 1 before serving
#
# When reimplementing:
# 1. Check official docker-compose for proper volume mounts
# 2. May need to mount entire /home/node/app to writable volume
# 3. Consider using their docker image entrypoint properly
# 4. Test locally with docker first before k8s deployment
#
# Reference: https://github.com/SillyTavern/SillyTavern
