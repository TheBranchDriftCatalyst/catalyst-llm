---
# OpenClaw - Self-hosted AI personal assistant gateway
# https://github.com/openclaw/openclaw
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openclaw
  labels:
    app: openclaw
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/part-of: catalyst-llm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: openclaw
  template:
    metadata:
      labels:
        app: openclaw
        app.kubernetes.io/name: openclaw
      annotations:
        backup.velero.io/backup-volumes: "openclaw-data"
    spec:
      # Merge infrastructure config into PVC so OpenClaw can still write to it
      initContainers:
        - name: merge-config
          image: alpine/openclaw:latest
          securityContext:
            runAsUser: 0
          command:
            - "node"
            - "-e"
            - |
              const fs = require('fs');
              const { execSync } = require('child_process');
              // Recursive deep merge: infra values override, user values preserved
              function deepMerge(target, source) {
                for (const key of Object.keys(source)) {
                  if (source[key] && typeof source[key] === 'object' && !Array.isArray(source[key])
                      && target[key] && typeof target[key] === 'object' && !Array.isArray(target[key])) {
                    deepMerge(target[key], source[key]);
                  } else {
                    target[key] = source[key];
                  }
                }
                return target;
              }
              const infra = JSON.parse(fs.readFileSync('/tmp/openclaw-defaults/openclaw.json', 'utf8'));
              const configPath = '/home/node/.openclaw/openclaw.json';
              let existing = {};
              try { existing = JSON.parse(fs.readFileSync(configPath, 'utf8')); } catch {}
              const merged = deepMerge(existing, infra);
              fs.writeFileSync(configPath, JSON.stringify(merged, null, 2));
              execSync('chown 1000:1000 /home/node/.openclaw/openclaw.json');
              console.log('Config merged successfully');
          volumeMounts:
            - name: openclaw-data
              mountPath: /home/node/.openclaw
            - name: openclaw-config
              mountPath: /tmp/openclaw-defaults
      containers:
        - name: openclaw
          image: alpine/openclaw:latest
          imagePullPolicy: IfNotPresent
          # Image CMD: node dist/index.js --allow-unconfigured
          # gateway.bind: "lan" set in config (container default)
          args: ["node", "dist/index.js", "gateway", "--allow-unconfigured"]
          env:
            - name: OPENCLAW_GATEWAY_TOKEN
              valueFrom:
                secretKeyRef:
                  name: openclaw-secrets
                  key: gateway-token
            # Route all LLM requests through litellm proxy
            - name: OPENAI_API_BASE
              value: "http://litellm:4000"
            - name: ANTHROPIC_API_BASE
              value: "http://litellm:4000"
            # API key for litellm (master key)
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: litellm-secrets
                  key: master-key
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: litellm-secrets
                  key: master-key
            # Notion API key for Notion skill
            - name: NOTION_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openclaw-secrets
                  key: notion-api-key
          ports:
            - name: http
              containerPort: 18789
              protocol: TCP
          volumeMounts:
            - name: openclaw-data
              mountPath: /home/node/.openclaw
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
      volumes:
        - name: openclaw-data
          persistentVolumeClaim:
            claimName: openclaw-data
        - name: openclaw-config
          configMap:
            name: openclaw-config
