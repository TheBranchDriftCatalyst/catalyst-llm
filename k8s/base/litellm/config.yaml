model_list:
  # ───────────────────────────────────────────────────────────────────
  # Hosted providers
  # ───────────────────────────────────────────────────────────────────

  # --- Anthropic (Claude) ---
  # May 2026: Opus 4.7 (released 2026-04-16, 87.6% SWE-bench Verified) is
  # the flagship — replaces opus-4 + sonnet-4 from May 2025. Haiku 4.5 is
  # the budget pair. SOTA shortlist lives in packages/mac-node/AGENTS.md.
  - model_name: "claude-opus-4-7"
    litellm_params:
      model: "anthropic/claude-opus-4-7"
      api_key: "os.environ/ANTHROPIC_API_KEY"
  - model_name: "claude-haiku-4-5-20251001"
    litellm_params:
      model: "anthropic/claude-haiku-4-5-20251001"
      api_key: "os.environ/ANTHROPIC_API_KEY"

  # --- OpenAI (GPT) ---
  # May 2026: gpt-5.5 (GA 2026-04-24, 1M context) replaces gpt-4o + 4o-mini.
  # o3-mini stays as the OpenAI reasoning option (NOTE: it can return empty
  # content if max_tokens is small — reasoning tokens count against the cap.
  # Clients should send max_tokens >= 4096 when targeting it).
  - model_name: "gpt-5.5"
    litellm_params:
      model: "openai/gpt-5.5"
      api_key: "os.environ/OPENAI_API_KEY"
  - model_name: "o3-mini"
    litellm_params:
      model: "openai/o3-mini"
      api_key: "os.environ/OPENAI_API_KEY"
      # Floor max_tokens so reasoning doesn't burn the budget and yield ""
      max_tokens: 4096

  # --- OpenAI Embeddings ---
  - model_name: "text-embedding-3-small"
    litellm_params:
      model: "openai/text-embedding-3-small"
      api_key: "os.environ/OPENAI_API_KEY"
  - model_name: "text-embedding-3-large"
    litellm_params:
      model: "openai/text-embedding-3-large"
      api_key: "os.environ/OPENAI_API_KEY"

  # ───────────────────────────────────────────────────────────────────
  # RunPod (custom vLLM serverless endpoints)
  # ───────────────────────────────────────────────────────────────────

  # >>> runpod-vllm:auto-generated:start <<<
  # DO NOT EDIT — managed by docker/runpod-vllm/scripts/gen-litellm.py
  # Source of truth: docker/runpod-vllm/endpoints.yaml (0 deployed, 2 pending)
  # Regenerate: cd docker/runpod-vllm && task generate

  # TODO (endpoint not yet created: qwen3.6-35b-a3b) — uncomment after setting runpod_endpoint_id
  # - model_name: runpod/qwen3.6-35b-a3b
  #   litellm_params:
  #     model: openai/qwen3.6-35b-a3b
  #     api_base: https://api.runpod.ai/v2/<ENDPOINT_ID>/openai/v1
  #     api_key: os.environ/RUNPOD_API_KEY
  #     timeout: 300
  #   model_info:
  #     description: Qwen3.6-35B-A3B MoE (35B total / ~3B active) with Hermes tool parser and 1-layer MTP speculative decoding.
  #       Best $/throughput for tool-calling workloads on RunPod serverless.

  # TODO (endpoint not yet created: qwen3.6-27b) — uncomment after setting runpod_endpoint_id
  # - model_name: runpod/qwen3.6-27b
  #   litellm_params:
  #     model: openai/qwen3.6-27b
  #     api_base: https://api.runpod.ai/v2/<ENDPOINT_ID>/openai/v1
  #     api_key: os.environ/RUNPOD_API_KEY
  #     timeout: 300
  #   model_info:
  #     description: Qwen3.6-27B dense model with Hermes tool parser and MTP speculative decoding. Highest format adherence per
  #       token — best for the initial tool-calling pass when paired with the 35B-A3B for repair loops.
  # >>> runpod-vllm:auto-generated:end <<<

  # NOTE: runpod/gemma-4-31b-it removed — endpoint fa625vcnpdv1vr returns
  # 404 (pod offline as of May 2026 audit). Re-add with a fresh endpoint
  # ID if you bring runpod back up.

  # >>> mac-node:auto-generated:start <<<
  # DO NOT EDIT — managed by packages/mac-node/scripts/gen-litellm.py
  # Source of truth: packages/mac-node/models.yaml (16 ollama, 0 vllm-mlx, 1 image_gen)
  # Regenerate: cd packages/mac-node && task generate

  # ─── Ollama (Metal accelerated) ───────────────────────────────────
  - model_name: mac/qwen3-coder
    litellm_params:
      model: ollama/qwen3-coder:30b
      api_base: http://192.168.1.33:11434
    model_info:
      description: Qwen3-Coder 30B-A3B MoE — 256K ctx, ~Sonnet 4.5-class agentic coding, BFCL v3 top tier (function calling).
        Replaces qwen2.5-coder + devstral. - Mac M5 Max Metal
      supports_function_calling: true

  - model_name: mac/deepseek-r1
    litellm_params:
      model: ollama/deepseek-r1:32b
      api_base: http://192.168.1.33:11434
    model_info:
      description: DeepSeek R1 Distill Qwen 32B — beats o1-mini on math/code reasoning, ~18-20GB Q4_K_M, 11-14 tok/s on M5 Max.
        The reasoning local sweet spot. - Mac M5 Max Metal
      supports_reasoning: true

  - model_name: mac/gemma4-26b
    litellm_params:
      model: ollama/gemma4:26b
      api_base: http://192.168.1.33:11434
    model_info:
      description: Gemma 4 26B-A4B MoE (multimodal + thinking, Q4_K_M). Vision routing alias for the playground until qwen3-vl
        is pulled and verified. - Mac M5 Max Metal
      supports_vision: true

  - model_name: mac/gemma4-vision
    litellm_params:
      model: ollama/gemma4:26b
      api_base: http://192.168.1.33:11434
    model_info:
      description: alias of mac/gemma4-26b (gemma4:26b) - Mac M5 Max Metal
      supports_vision: true

  - model_name: mac/gemma4
    litellm_params:
      model: ollama/gemma4:e4b
      api_base: http://192.168.1.33:11434
    model_info:
      description: Gemma 4 E4B MoE (multimodal + thinking) — fast vision-capable daily. - Mac M5 Max Metal
      supports_vision: true

  - model_name: mac/phi4
    litellm_params:
      model: ollama/phi4:14b
      api_base: http://192.168.1.33:11434
    model_info:
      description: Phi-4 14B — punches above its weight, default daily driver under 14B. - Mac M5 Max Metal
      supports_reasoning: true

  - model_name: mac/behemoth-x
    litellm_params:
      model: ollama/behemoth-x:Q4_K_M
      api_base: http://192.168.1.33:11434
    model_info:
      description: Behemoth-X 123B v2 (Mistral Large 2411 finetune) — long-form literary champion; stable prose to ~19k tokens.
        Use Metharme/Mistral system tokens; min_p 0.05. - Mac M5 Max Metal

  - model_name: mac/hermes-4-70b
    litellm_params:
      model: ollama/hf.co/unsloth/Hermes-4-70B-GGUF:Q4_K_M
      api_base: http://192.168.1.33:11434
    model_info:
      description: Hermes-4 70B (Llama 3.1 70B base) — top RefusalBench score, hybrid <think> toggle, lowest AI-tells baseline.
        Toggle reasoning OFF for prose. ChatML. - Mac M5 Max Metal
      supports_reasoning: true

  - model_name: mac/cydonia-24b
    litellm_params:
      model: ollama/hf.co/TheDrummer/Cydonia-24B-v4.3-GGUF:Q5_K_M
      api_base: http://192.168.1.33:11434
    model_info:
      description: Cydonia 24B v4.3 (Mistral Small 3.2 base, Dec 2025) — Drummer's most-improved-prose release; fast 24B daily
        for RP. Mistral v7 Tekken template; temp 1.0 / min_p 0.05 / rep_pen 1.03. - Mac M5 Max Metal

  - model_name: mac/qwen3-moe-uncensored
    litellm_params:
      model: ollama/qwen3-moe-uncensored:Q4_K_M
      api_base: http://192.168.1.33:11434
    model_info:
      description: Qwen3-30B-A3B MoE (3B active) — heretic-abliterated reasoning distill. MoE wildcard — small-model speeds
        with 30B breadth. Reddit-hot but bench-thin; experimental A/B candidate. ChatML; temp 0.7. - Mac M5 Max Metal

  - model_name: mac/magnum-v4
    litellm_params:
      model: ollama/magnum-v4:Q4_K_M
      api_base: http://192.168.1.33:11434
    model_info:
      description: Magnum v4 72B (Qwen2.5 72B finetune) — gold standard for explicit + literary craft prose voice. ChatML; 16k
        context (shorter); temp 1.0 / min_p 0.1. - Mac M5 Max Metal

  - model_name: mac/nuextract2
    litellm_params:
      model: ollama/nuextract2:latest
      api_base: http://192.168.1.33:11434
    model_info:
      description: NuExtract-2.0-8B (Qwen2.5-VL based) — beats GPT-4.1 by +9 F-score on schema extraction. Generalist extractor.
        - Mac M5 Max Metal
      supports_vision: true

  - model_name: mac/nuextract1.5
    litellm_params:
      model: ollama/nuextract1.5:latest
      api_base: http://192.168.1.33:11434
    model_info:
      description: 'NuExtract 1.5 — template-based JSON fill (### Template / ### Text shape). Kept for legacy prompts. - Mac
        M5 Max Metal'

  - model_name: mac/universalner
    litellm_params:
      model: ollama/universalner:latest
      api_base: http://192.168.1.33:11434
    model_info:
      description: UniversalNER 7B — zero-shot named-entity recognition ("What describes <type> in the text?"). Specialist for
        ad-hoc NER. - Mac M5 Max Metal

  - model_name: mac/qwen3-embed-8b
    litellm_params:
      model: ollama/qwen3-embedding:8b
      api_base: http://192.168.1.33:11434
    model_info:
      description: 'Qwen3 Embedding 8B — #1 MTEB multilingual at 70.58, 100+ langs, dims 32-4096. The single embedding model
        we need. - Mac M5 Max Metal'
      mode: embedding

  - model_name: mac/qwen3-vl
    litellm_params:
      model: ollama/qwen3-vl:latest
      api_base: http://192.168.1.33:11434
    model_info:
      description: Qwen3-VL — beats InternVL3 on DocVQA / OS-World, 32-language OCR, robust to blur/tilt. May 2026 vision SOTA
        for local. - Mac M5 Max Metal
      supports_vision: true

  # ─── vLLM-MLX (high throughput, OpenAI-compatible) ────────────────

  # ─── Image generation (ComfyUI shim, OpenAI-compatible) ───────────
  - model_name: mac/flux-krea
    litellm_params:
      model: openai/dev-krea
      api_base: http://192.168.1.33:8012/v1
      api_key: not-needed
    model_info:
      description: FLUX.1 Krea Dev — May 2026 SOTA personal-use image-gen, MLX-native via mflux (~12-18s/image on M5 Max, 1024²
        @ 20 steps, Q8) via mflux shim - Mac M5 Max Metal
      mode: image_generation
  # >>> mac-node:auto-generated:end <<<

general_settings:
  master_key: "os.environ/LITELLM_MASTER_KEY"
  database_url: "os.environ/DATABASE_URL"
  store_model_in_db: true
  store_prompts_in_spend_logs: true
  # SSO Configuration - allow both SSO and master key login
  ui_access_mode: "all"
  # Make SSO users proxy admins by default
  default_user_params:
    user_role: "proxy_admin"

litellm_settings:
  drop_params: true
  num_retries: 3
  request_timeout: 120
